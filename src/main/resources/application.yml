server:
  port: 1111

spring:
  thymeleaf:
    prefix: classpath:templates/
    suffix: .html
    check-template-location: true
    mode: HTML
    cache: false

  datasource:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: admin
      password: 1234
      url: jdbc:mysql://localhost:3306/playbours?serverTimezone=UTC&characterEncoding=UTF-8

  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        show-sql: true

  ##data:
    ##mongodb:
      ##uri: mongodb+srv://admin:zzz1234@playcluster.a1hqkve.mongodb.net/Playbours?retryWrites=true&w=majority

  ##datasource:
    ##driver-class-name: org.mariadb.jdbc.Driver
    ##javaUrl: jdbc:mariadb://svc.sel4.cloudtype.app:32586/Playbours?characterEncoding=UTF-8&serverTimezone=UTC
    ##username: test
    ##password: zxcv1234

  # 파일업로드 관련
  servlet:
    multipart:
      enabled: true
      # 파일 하나당 최대 업로드 가능 용량
      max-file-size: 10MB
      # 한번에 최대 업로드 가능 용량
      max-request-size: 50MB

# 파일업로드 경로
file:
  upload:
    path: 'C:\Playbours\attach\upload\'

logging:
  level:
    root: info